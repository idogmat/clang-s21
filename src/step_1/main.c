#include <stdio.h>
#include "divide.h"
#include "stdin.h"

int old_value = 33;

int main(int argc, char *argv[]) {
    printf("old_value = %d  |   new_value = %d  | divide_new_value = %d\n", old_value, new_value, divide_new_value(new_value, 2));
    calc(argc, argv);
    return 0;
}